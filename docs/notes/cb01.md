# CB01

**URL:** `cb01uno.digital`  
**Codice:** `src/cb01/`  
**Stato:** ✅ Funzionante

## Funzionamento
- Ricerca via `?s=query` (WordPress search)
- Parsing pagina risultati → pagina serie → episodi
- Player: MixDrop, MaxStream (via Uprot), stayonline.pro resolver

## Catena Estrazione
1. Search CB01 → pagina serie
2. Parse sp-head + sp-body per link player
3. stayonline.pro → risolve a URL reale (uprot, mixdrop, ecc.)
4. Uprot: captcha auto-solve → redirect chain → MaxStream HLS
5. MixDrop: unpacker JavaScript → URL diretto

## Fix Importanti (v3.0.55-59)
- `normalizeForSearch`: codifica `&` con `encodeURIComponent`
- `extractSeriesStreams`: aggiunto parsing sp-body (link folder `/msfld/`)
- `resolveFolder()`: risolve cartelle uprot in link individuali

## Path Uprot
| Path | Tipo | Captcha |
|------|------|---------|
| `/msf/` | File singolo | 3 digit, PHPSESSID (cached 22h) |
| `/mse/` | → Redirect a `/msf/` | Come sopra |
| `/msei/` | Episodio | 4 digit, token-based (no cache) |
| `/msfi/` | Item da folder | Condivide sessione `/msf/` |
| `/msfld/` | Cartella | Risolve a lista `/msfi/` |

## Variabili Env
- `PROXY_URL` — Per bypass CF su uprot.net
- `CF_WORKER_AUTH` — Per KV cookie uprot
